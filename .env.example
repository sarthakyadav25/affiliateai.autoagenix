# Environment Variables for Affiliate AutoAgenix

# Supabase Configuration
# Get these from your Supabase project settings > API
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Note: Create a 'traffic_logs' table in Supabase with the following SQL:
# 
# CREATE TABLE traffic_logs (
#   id BIGSERIAL PRIMARY KEY,
#   visitor_type TEXT NOT NULL CHECK (visitor_type IN ('ai_crawler', 'general_crawler', 'human')),
#   path TEXT NOT NULL,
#   user_agent TEXT NOT NULL,
#   created_at TIMESTAMPTZ DEFAULT NOW(),
#   ip_address TEXT,
#   country TEXT
# );
#
# CREATE INDEX idx_traffic_logs_visitor_type ON traffic_logs(visitor_type);
# CREATE INDEX idx_traffic_logs_created_at ON traffic_logs(created_at DESC);
